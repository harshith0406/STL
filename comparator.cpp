#include <bits/stdc++.h>
using namespace std;
int main()
{
  return 0;
  // this is quite complicated we can pass the third argument to the array such that it accepts and the value will be customized according to the behavior
}